# Notes
```
/chat - name the chat - can be listed and resumed
@<filename> specify precise file locations]
gemini -p "do this"  <-- headless mode
!    <-- toggle shell mode (run commands)
--yolo   <-- you only live once, turn off confirmations
ctrl-f   <-- focus shell
"can you explain the architecture of this project"    <-- typical use case
~/.gemini/commands /test.toml    <-- custom command. Can be accessed by "/test" on cli

```
